import { View } from "react-native";
import homepage_topbar_elem from "../../../styles/home/homepage_topbar_elem";

const HomePageBottomBarElem = (icon) => {
  return <View>{/* Icon with icon.props path instead of template */}</View>;
};

export default HomePageBottomBarElem;
