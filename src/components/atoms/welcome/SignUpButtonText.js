import { Text } from "react-native";
import I18n from "../../../i18n";

const SignUpButtonText = () => {
  return <Text>{I18n.t("SignUpButtonText.js")}</Text>; // Wtf is this
};

export default SignUpButtonText;
