import title from "../../../styles/welcome/title";
import { Text } from "react-native";

const WelcomeTitle = () => {
  return <Text style={title.title}>Haste</Text>;
};

export default WelcomeTitle;
