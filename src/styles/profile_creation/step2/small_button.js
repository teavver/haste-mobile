import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    width: 60,
    height: 40,
    marginHorizontal: 12,
    borderWidth: 1,
    justifyContent: "center",
    alignItems: "center",
    },
});
